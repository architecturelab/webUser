apiVersion: v1
kind: Service
metadata:
  name: web-user-lb
spec:
  type: LoadBalancer
  loadBalancerIP: "34.111.211.192"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
  selector:
    role: web-user