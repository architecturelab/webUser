apiVersion: v1
kind: Service
metadata:
  name: web-user-lb
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "web-static-ip"
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
  selector:
    role: web-user
  